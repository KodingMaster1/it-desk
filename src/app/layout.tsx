import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'IT-DMS - IT Department Management System',
  description: 'A comprehensive IT department management solution for tracking assets, vendors, requisitions, and more.',
  keywords: ['IT Management', 'Asset Tracking', 'IT Department', 'Inventory Management'],
  authors: [{ name: 'KodingMaster1' }],
  viewport: 'width=device-width, initial-scale=1',
  robots: 'index, follow',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  );
} 